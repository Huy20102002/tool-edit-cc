<UserControl x:Class="AutoVideoEditor.App.Controls.WaveformControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="800">
    <Border Background="#121215" BorderBrush="#27272a" BorderThickness="1" CornerRadius="8" Padding="10">
        <Grid>
            <!-- Canvas where waveform bars are rendered -->
            <Canvas x:Name="WaveformCanvas" ClipToBounds="True" SizeChanged="WaveformCanvas_SizeChanged" />
            
            <!-- Empty state placeholder -->
            <TextBlock x:Name="PlaceholderText"
                       Text="Chưa có dữ liệu sóng âm (Chọn hoặc phân tích một file giọng đọc)"
                       Foreground="#71717a"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontStyle="Italic" />
        </Grid>
    </Border>
</UserControl>
