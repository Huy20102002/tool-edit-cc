<UserControl x:Class="AutoVideoEditor.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:enums="clr-namespace:AutoVideoEditor.Core.Enums;assembly=AutoVideoEditor.Core"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Storage & Directories -->
            <Border Grid.Row="0" Background="#18181b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="📁 Thư mục lưu trữ &amp; Xuất file" FontSize="16" FontWeight="Bold" Foreground="#f4f4f5" Margin="0,0,0,16" />

                    <!-- Output Folder -->
                    <TextBlock Text="Thư mục xuất video mặc định:" Foreground="#a1a1aa" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Height="38" />
                        <Button Grid.Column="1" Content="Chọn thư mục" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ChooseOutputFolderCommand}" Margin="8,0,0,0" Height="38" />
                    </Grid>

                    <!-- Temp Folder -->
                    <TextBlock Text="Thư mục file tạm (Temp):" Foreground="#a1a1aa" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding TempDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Height="38" />
                        <Button Grid.Column="1" Content="Chọn thư mục" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ChooseTempFolderCommand}" Margin="8,0,0,0" Height="38" />
                    </Grid>

                    <!-- Naming Pattern & Overwrite -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Định dạng tên file xuất:" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <TextBox Text="{Binding OutputNamingPattern, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" />
                            <TextBlock Text="Thẻ hỗ trợ: {original_name}, {voice_name}, {index}, {date}" FontSize="11" Foreground="#71717a" Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Xử lý khi trùng tên file:" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <ComboBox SelectedValue="{Binding OverwritePolicy}" SelectedValuePath="Tag" Height="38">
                                <ComboBoxItem Content="Tự động đổi tên (_001, _002...)" Tag="{x:Static enums:OverwritePolicy.AutoRename}" />
                                <ComboBoxItem Content="Ghi đè file cũ" Tag="{x:Static enums:OverwritePolicy.Overwrite}" />
                                <ComboBoxItem Content="Bỏ qua file đã tồn tại" Tag="{x:Static enums:OverwritePolicy.Skip}" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Performance & Hardware Acceleration -->
            <Border Grid.Row="2" Background="#18181b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="⚡ Hiệu năng &amp; Tăng tốc phần cứng (GPU)" FontSize="16" FontWeight="Bold" Foreground="#f4f4f5" Margin="0,0,0,16" />

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Parallel Jobs -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding MaxParallelJobs, StringFormat='Số luồng xử lý đồng thời: {0} video'}" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <Slider Minimum="1" Maximum="16" Value="{Binding MaxParallelJobs}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,6,0,0" />
                            <TextBlock Text="Khuyên dùng: 2 - 4 luồng để máy tính mượt mà" FontSize="11" Foreground="#71717a" Margin="0,4,0,0" />
                        </StackPanel>

                        <!-- Hardware Encoder Preference -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Ưu tiên bộ mã hóa (Hardware Encoder):" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <ComboBox SelectedValue="{Binding HardwareEncoder}" SelectedValuePath="Tag" Height="38">
                                <ComboBoxItem Content="Tự động phát hiện (Auto GPU / CPU)" Tag="{x:Static enums:HardwareEncoderType.Auto}" />
                                <ComboBoxItem Content="NVIDIA NVENC (GeForce / RTX)" Tag="{x:Static enums:HardwareEncoderType.NvidiaNvenc}" />
                                <ComboBoxItem Content="AMD AMF (Radeon)" Tag="{x:Static enums:HardwareEncoderType.AmdAmf}" />
                                <ComboBoxItem Content="Intel QuickSync (QSV)" Tag="{x:Static enums:HardwareEncoderType.IntelQsv}" />
                                <ComboBoxItem Content="CPU tiêu chuẩn (libx264)" Tag="{x:Static enums:HardwareEncoderType.CPU}" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Button Content="🔍 Kiểm tra khả năng tăng tốc phần cứng GPU" Style="{StaticResource CyanButtonStyle}" Command="{Binding CheckHardwareAccelerationCommand}" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <!-- Silence Detection Defaults -->
            <Border Grid.Row="4" Background="#18181b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="🎙️ Thông số phát hiện khoảng lặng mặc định" FontSize="16" FontWeight="Bold" Foreground="#f4f4f5" Margin="0,0,0,16" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Ngưỡng âm lượng (dB):" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <TextBox Text="{Binding DefaultSilenceThresholdDb, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Khoảng lặng tối thiểu (ms):" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <TextBox Text="{Binding DefaultMinSilenceMs, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" />
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Khoảng đệm câu (ms):" Foreground="#a1a1aa" Margin="0,0,0,6" />
                            <TextBox Text="{Binding DefaultPaddingBeforeMs, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Save Button -->
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Lưu tất cả cài đặt" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveSettingsCommand}" Padding="28,12" FontSize="15" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
