<UserControl x:Class="AutoVideoEditor.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>

        <!-- Top Action Bar -->
        <Border Grid.Row="0" Background="#18181b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="📜 Nhật Ký Hoạt Động &amp; Chi Tiết Kỹ Thuật (Logs)" FontSize="16" FontWeight="Bold" Foreground="#f4f4f5" />
                    <TextBlock Text="Toàn bộ lệnh FFmpeg, exit code và thông báo phân tích được lưu trữ đầy đủ tại đây." FontSize="12" Foreground="#a1a1aa" Margin="0,2,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📋 Sao chép tất cả" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CopyAllLogsCommand}" Margin="0,0,8,0" />
                    <Button Content="📂 Mở thư mục logs" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenLogsFolderCommand}" Margin="0,0,8,0" />
                    <Button Content="🗑️ Xóa nhật ký" Style="{StaticResource DangerButtonStyle}" Command="{Binding ClearLogsCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Log List DataGrid -->
        <Border Grid.Row="2" Background="#18181b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="12">
            <DataGrid ItemsSource="{Binding Logs}" SelectedItem="{Binding SelectedLog}" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                      RowBackground="#18181b" AlternatingRowBackground="#121215" Foreground="#f4f4f5"
                      HorizontalGridLinesBrush="#27272a" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Thời gian" Binding="{Binding FormattedTimestamp}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#71717a" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Mức độ" Binding="{Binding Level}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="ERROR">
                                        <Setter Property="Foreground" Value="#ef4444" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="WARN">
                                        <Setter Property="Foreground" Value="#f59e0b" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="INFO">
                                        <Setter Property="Foreground" Value="#06b6d4" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Job ID" Binding="{Binding JobId}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#a1a1aa" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Nội dung thông báo" Binding="{Binding Message}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Log Details Box -->
        <Border Grid.Row="4" Background="#09090b" BorderBrush="#27272a" BorderThickness="1" CornerRadius="12" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Chi tiết dòng log được chọn:" FontSize="12" FontWeight="SemiBold" Foreground="#a1a1aa" Margin="0,0,0,6" />
                <TextBox Grid.Row="1" Text="{Binding SelectedLog.Details, Mode=OneWay}" IsReadOnly="True"
                         Background="#121215" Foreground="#f4f4f5" BorderThickness="0" FontFamily="Consolas, monospace"
                         FontSize="12" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
